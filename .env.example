# WebMCP browser-agent proxy. Provision a read-only scoped RepWell API key.
# Rotate by replacing the Vercel env var value and redeploying.
WEBMCP_INTERNAL_API_KEY=

# Better Auth is the only auth system after the BA cutover.
# Required in production/Vercel.
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=
