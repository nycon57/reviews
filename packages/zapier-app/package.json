{
  "name": "@repwell/zapier-app",
  "version": "1.0.0",
  "private": true,
  "description": "RepWell Zapier Platform CLI integration",
  "main": "index.js",
  "scripts": {
    "build": "npm run validate && npm run typecheck",
    "test": "node --test test/*.test.js",
    "typecheck": "tsc --noEmit --project tsconfig.json",
    "validate": "zapier-platform validate"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "zapier-platform-core": "19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "nock": "^14.0.10",
    "typescript": "5.9.3",
    "zapier-platform-cli": "^19.0.0"
  }
}
