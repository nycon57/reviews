import type { CSSProperties } from 'react';

/**
 * Convenience type for inline style props used across email builder blocks.
 * Keeps the original Waypoint pattern of passing CSSProperties.
 */
export type TStyle = CSSProperties;
